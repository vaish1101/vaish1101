# Everything needed to build, validate and preview the profile. No secrets or network access required.
# Python 3.13 (CI uses the same). Pinned for reproducible output.
lxml==6.1.3
Pillow==12.3.0
resvg-py==0.5.0
